require('dotenv').config(); const { S3Client, ListObjectsV2Command } = require('@aws-sdk/client-s3'); const client = new S3Client({ region: process.env.S3_REGION, endpoint: process.env.S3_ENDPOINT, credentials: { accessKeyId: process.env.S3_ACCESS_KEY, secretAccessKey: process.env.S3_SECRET_KEY }, forcePathStyle: true }); client.send(new ListObjectsV2Command({ Bucket: process.env.S3_BUCKET })).then(data => console.log('Bucket contents:', data.Contents?.length || 0, 'items')).catch(err => console.error('Error:', err.message));
